name: Deploy to GitHub Pages

on:
  push:
    branches:
      - main
      - master

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: front-end/package-lock.json

      - name: Install dependencies
        working-directory: front-end
        run: npm ci

      - name: Set API URL
        working-directory: front-end
        env:
          API_URL: ${{ secrets.API_URL }}
        run: |
          if [ -z "$API_URL" ]; then
            echo "::warning::API_URL secret is not set. Using placeholder. Set it in Settings → Secrets → Actions before deploying."
            API_URL="https://api.example.com"
          fi
          sed -i "s|__API_URL__|$API_URL|g" src/environments/environment.prod.ts

      - name: Build
        working-directory: front-end
        run: npm run build -- --configuration production

      - name: Copy index.html to 404.html (SPA routing fix)
        run: cp front-end/dist/dooitt/index.html front-end/dist/dooitt/404.html

      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: front-end/dist/dooitt
          cname: ${{ secrets.CNAME }}
